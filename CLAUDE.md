# 时薪计（time-wage）

一个给打工人看「今天实时赚了多少 / 加班如何稀释时薪」的网页小工具。面向国内用户，移动端为主，目前是免后端的内测体验版。

## 技术栈与约束
- **单文件**：所有 HTML / CSS / JS 都写在 `index.html` 里，无构建步骤、无框架、无后端。改动直接编辑这一个文件。
- **部署**：推送到 GitHub → Netlify 自动部署。首页文件名必须是 `index.html`，不要改名。
- **数据存储**：浏览器 localStorage。
  - `wage_settings_v1`：用户设置（月薪、时薪、上下班/午休/加班费起算时间、加班倍率、模式、加班费勾选）。每次改动即保存，打开时恢复。
  - `wage_daily_log_v1`：每日小结历史，用于趋势图和与前一日对比。
  - localStorage 不可用时降级为内存变量（claude.ai / 沙箱预览环境会触发），并提示用户下载到本地使用。
- **绝对不要用 emoji**：部分设备不支持，会显示成乱码或「月亮」。金币等图形一律用 CSS 绘制。
- 界面为中文。等宽数字用 JetBrains Mono，中文用系统字体。整体为工业极简深色风（墨蓝 + 水泥灰，红色作警示）。

## 核心产品逻辑
- **两种计薪模式**：固定月薪 / 时薪，由 `state.mode` 控制。
- **工作日 / 休息日**：默认按当天星期自动判断，可手动切换。2026 年法定节假日与调休补班已内置在 `HOLIDAYS_2026` / `MAKEUPS_2026`（注意：跨年后需要补当年数据，否则回退到只按周末算）。
- **计薪天数**：默认按当月实际工作日自动计算（扣法定假、含调休补班），用户可手动改，可点「↻ 按本月」重算。优先恢复用户上次填写的值。
- **加班费相关设置联动**：加班费起算时间、缓冲期、加班倍率等（HTML 里 class 为 `.otonly` 的元素），仅在「提供加班费」时（`isPaid`）显示并生效；没勾加班费时隐藏。
- **稀释状态（代码里的 `drain`）**：月薪 + 不提供加班费 + 加班或休息日白干时，主数字从「今天已赚」切换为「当日真实时薪」并标红下沉，下方固定显示「正在白干流失 ¥X」实时累加，凸显时薪被稀释。
- **进账动效**：进账时 CSS 金色圆币上浮（`spawnCoin`，纯 CSS 绘制，非 emoji）。
- **跨日 / 通宵**：用户手动勾选「通宵 / 跨日」并指定真实上班时刻（不是按点击打卡，因为常常干到半夜才意识到要通宵）。按绝对时长累计，下班时间、加班费起算时间都支持跨日（填 02:00 表示次日凌晨）。

## 关键函数
- `compute()`：核心计算。区分工作日/休息日，用「实际在岗区间 ∩ 计划时段」求交集得到正常/加班/缓冲时长，返回 phase、收入、真实时薪等。
- `render()`：每 200ms 刷新界面与动效模式。
- `saveSettings()` / `loadSettings()`：设置持久化。
- `fmt()` 金额格式化、`fmtDurT()` 时长格式化（支持跨日大时长）。

## 工作约定
- 每次改完，提交并 push，让 Netlify 自动部署。
- **每次完成任务后，自动 commit 并 push 到 GitHub，无需用户再次提醒。**
- 改动后在移动端窄屏（如 375px）和桌面宽度都自测一遍再提交。
- 保持单文件、无依赖、无 emoji 这几条底线。

## 后续计划
- 接腾讯云开发 CloudBase（匿名登录 + 云数据库）实现跨设备同步，未来支持微信扫码登录，小程序作为补充入口。
